import { SetMoviePathPipe } from './set-movie-path.pipe';

describe('SetMoviePathPipe', () => {
  it('create an instance', () => {
    const pipe = new SetMoviePathPipe();
    expect(pipe).toBeTruthy();
  });
});
